package com.Rev.RevStay.models;

/**
 * Enum representing the types of users in the system.
 * 
 * This enum is used to differentiate between different roles or types of users:
 * - `USER`: Represents a regular user of the system.
 * - `OWNER`: Represents a hotel owner in the system.
 */
public enum UserType {
        USER, OWNER
}
